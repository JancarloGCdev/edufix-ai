import type { UserRole } from "@/src/types/auth";

export type { UserRole } from "@/src/types/auth";
export {
  ALLOWED_EMAIL_DOMAIN,
  DEFAULT_USER_ROLE,
  USER_ROLES,
  isUserRole,
} from "@/src/types/auth";
